# ---------------------------
# MySQL Database Configuration
# ---------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/skillhub?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=NewPassword123!

# ---------------------------
# JPA / Hibernate Configuration
# ---------------------------
spring.jpa.hibernate.ddl-auto=update       # Automatically updates schema (dev use)
spring.jpa.show-sql=true                   # Show SQL queries in console

# ---------------------------
# Thymeleaf Configuration
# ---------------------------
spring.thymeleaf.cache=false               # Disable template caching during dev

# ---------------------------
# Server Configuration
# ---------------------------
server.port=8080

